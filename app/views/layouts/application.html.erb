<!DOCTYPE html>
<html>
  <head>
    <title>Rastreamento - Icasei</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>
  <body>
    <nav class="navbar navbar-default navbar-static-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand">Rastreamento ©‎</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li <% if controller_name == "home" %>class="active"<% end -%>><%= link_to 'Home', {:controller => 'home', :action => 'index'} %></li>
            <li <% if controller_name == "sobre" %>class="active"<% end -%>><%= link_to 'Sobre', {:controller => 'sobre', :action => 'index'} %></li>
            <li <% if controller_name == "contato" %>class="active"<% end -%>><%= link_to 'Contato', {:controller => 'contato', :action => 'index'} %></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li <% if controller_name == "relatorio" %>class="active"<% end -%>><%= link_to 'Relatório de Acessos', {:controller => 'relatorio', :action => 'index'} %></li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
